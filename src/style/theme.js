import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#4B6587', // Indigo-like blue
        },
        secondary: {
            main: '#C8405F', // Traditional Japanese red
        },
    },
});